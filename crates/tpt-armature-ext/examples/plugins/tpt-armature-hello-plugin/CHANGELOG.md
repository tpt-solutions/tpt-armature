# Changelog

All notable changes to `tpt-armature-hello-plugin` are documented here.

The format is based on [Keep a Changelog](https://keepachangelog.com/).

## [Unreleased]

### Added
- Example Wasm plugin guest implementing the `tpt-armature` ABI
  (`log`, `get_instruction_count`, `rename`) and exporting `tpt_armature_run`.
- Demonstrates log emission and a symbol rename proposal against the host
  analysis.
